<%= form_for(@sudoku_topic, url: sudokus_path) do |f| %>
  <% if @sudoku_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sudoku_topic.errors.count, "error") %> prohibited this sudoku from being saved:</h2>

      <ul>
      <% @sudoku_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <%= f.text_field :topic %>
   <br>

<table>
<% @sudoku_topic.sudoku_rows.each do |row| %>
  <%= f.fields_for(row) do |r| %>
  <tr>
    <% row.sudoku_columns.each do |column| %>
      <% pp column %>
      <%= r.fields_for(column) do |c| %>
        <%#= c.text_field(:value) %>
      <% end %>
    <% end %>
  </tr>
  <% end %>
<% end%> 
</table>
<%for i in 1..9%>
  <%for i2 in 1..9%>

              <% mystring =i.to_s+","+i2.to_s %>
             <%=text_field_tag (i.to_s+","+i2.to_s) %>
           
<%
    end
 end
%>


<div class="actions">
    <%= f.submit %>
</div>
<% end %>